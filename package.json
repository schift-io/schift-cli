{
  "name": "@schift-io/cli",
  "version": "0.1.0",
  "description": "Schift CLI - Deploy and manage AI agents",
  "type": "module",
  "bin": {
    "schift": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --watch",
    "test": "vitest run"
  },
  "keywords": [
    "schift",
    "ai-agent",
    "cli",
    "deploy"
  ],
  "author": "Schift <hello@schift.io>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
